name: Final site layout

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Layout updater disabled; site is maintained by the single final site workflow."
